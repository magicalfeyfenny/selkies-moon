# Platform art masters

`mac_app_icon.kra` is the canonical Krita master for the shipped macOS icon.
The PNG inside the GameMaker options directory is a generated runtime export.

Reference images and platform metadata are not masters and must never be used
to overwrite this KRA during a normal build.
